# Deployment-only runtime dependencies, pinned to the versions this app was
# verified against. The fitted models are unpickled with joblib, so the
# scikit-learn pin is load-bearing, not cosmetic.
#
# Deliberately absent: pytest and ruff (development only), matplotlib, plotly,
# py7zr, reportlab and scipy-for-the-pipeline (used by the offline build
# scripts, which never run here - this app only reads cached artifacts).
streamlit==1.62.0
pandas==3.0.5
numpy==2.5.2
pyarrow==25.0.1
scikit-learn==1.9.0
joblib==1.6.0
duckdb==1.5.5
